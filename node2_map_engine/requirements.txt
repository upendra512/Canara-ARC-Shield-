# Node 2 — MAP Engine
# Offline rule-based analyzer; the vector clause retrieval uses the shared
# arc_vector store (Ollama embeddings + ChromaDB).
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
pydantic-settings>=2.0
chromadb>=1.0
